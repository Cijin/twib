{
  "name": "twib",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "watch-css": "tailwindcss -i ./public/css/input.css -o ./public/css/style.css --watch",
    "watch-templ": "templ generate --watch",
    "watch": "npm run watch-templ & npm run watch-css",
    "build-server": "go build -o server",
    "build-templ": "templ generate",
    "build-css": "tailwindcss -o ./public/css/style.css --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
